%h1 All active events
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - if @events.any?
      - @events.each do |event|
        %table.table
          %thead
            %tr
              %td
                = event.title
              %td
                = event.location
              %td
                = event.date_and_time.to_formatted_s(:long)